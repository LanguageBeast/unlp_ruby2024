<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <%= link_to 'Blog Page', root_path, class:'navbar-brand' %>    
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0 d-flex w-100">
        <li class="nav-item">
          <%= link_to 'Home', root_path, class:'nav-link lead' %>   
        </li>
        <li class="nav-item">
          <%= link_to 'See products', productos_path, class:"nav-link lead" %>
        </li>
        <% if current_user %>
          <li class="nav-item">
            <%= link_to 'See sales', sales_path, class:"nav-link lead" %>
          </li>
          <li class="nav-item">
            <%= link_to 'See users', users_path, class:"nav-link lead" %>
          </li>
          <li class="nav-item ms-auto d-flex">
            <p class="lead my-0 align-self-center mr-2">Hello, <%= current_user.username%>! </p>
           <%= button_to 'Log out', destroy_user_session_path, method: :delete, form: { data: { turbo: true, turbo_confirm: 'Are you sure you want to log out?' } }, class: 'btn btn-primary mt-1' %>
          </li>
        <% else %>
          <li class="nav-item ms-auto">
            <%= button_to 'Log in', new_user_session_path, method: :get,  class:"btn btn-primary mt-1" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>